- for trail <- @breadcrumb_trails do
  %ol.breadcrumb
    - for crumb <- trail do
      %li.breadcrumb-item
        - if crumb[:url] do
          = link crumb[:name], to: crumb[:url]
        - else
          = crumb[:name]
